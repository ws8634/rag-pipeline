Metadata-Version: 2.4
Name: llm_rag_pipeline
Version: 0.1.0
Summary: A reference RAG pipeline for local testing
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: click>=8.0
Requires-Dist: numpy>=1.21
Requires-Dist: faiss-cpu>=1.7.0
Requires-Dist: pytest>=7.0
Provides-Extra: openai
Requires-Dist: openai>=1.0; extra == "openai"
Provides-Extra: all
Requires-Dist: openai>=1.0; extra == "all"

# LLM RAG Pipeline

一个可在本地复现的RAG（检索增强生成）参考链路实现，包含切片、向量化、召回重排、生成四个核心环节。

## 功能特性

- **切片（Chunking）**：UTF-8友好的文本切片，支持自定义chunk大小和overlap
- **向量化（Embedding）**：可插拔的嵌入后端，默认使用确定性存根实现
- **召回重排（Retrieval & Rerank）**：基于FAISS的向量检索，支持TF-IDF和长度惩罚两种轻量级重排
- **生成（Generation）**：可插拔的生成后端，默认使用存根模式输出可解析结构

## 安装

```bash
# 从源码安装（开发模式）
pip install -e .

# 或安装所有可选依赖
pip install -e ".[all]"
```

## 快速开始

### 1. 准备数据

创建包含`.txt`或`.md`文件的目录：

```bash
mkdir -p ./data
echo "RAG 代表 Retrieval-Augmented Generation。这是一种将检索与生成相结合的技术。" > ./data/rag_intro.txt
echo "FAISS 是 Facebook 开发的高效向量相似度搜索库。" > ./data/faiss_info.txt
```

### 2. 构建索引（Ingest）

```bash
# 使用默认参数
rag ingest ./data

# 自定义chunk大小和overlap
rag ingest ./data --chunk-size 500 --overlap 50

# 指定索引存储目录
rag ingest ./data --index-dir ./my_index
```

### 3. 查询（Query）

```bash
# 直接传入问题
rag query "什么是 RAG？"

# 从stdin读取问题
echo "FAISS 是什么？" | rag query

# 使用自定义索引目录
rag query --index-dir ./my_index "什么是向量检索？"

# 关闭重排
rag query --no-rerank "RAG 的全称是什么？"
```

## CLI 命令详解

### `rag ingest` - 构建向量索引

**参数说明：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `DATA_DIR` | 位置参数 | - | 包含`.txt`和`.md`文件的目录 |
| `--index-dir, -i` | 选项 | `.rag_index` | 索引存储目录 |
| `--chunk-size, -s` | 选项 | 1000 | 每个chunk的字符数 |
| `--overlap, -o` | 选项 | 100 | chunk之间的重叠字符数 |
| `--embed-backend, -e` | 选项 | `stub` | 嵌入后端：`stub` 或 `openai` |
| `--embed-dim` | 选项 | 128 | 存根后端的向量维度 |
| `--on-conflict` | 选项 | `skip` | 重复chunk处理策略：`skip` 或 `fail` |
| `--clear` | 标志 | False | 先清空现有索引 |

**退出码含义：**

| 退出码 | 含义 |
|--------|------|
| 0 | 成功 |
| 1 | 目录不存在或参数错误 |
| 2 | 目录下没有可读文件 |
| 3 | 所有文档解析失败或为空 |
| 4 | 索引兼容性错误（维度或后端不匹配） |
| 5 | 重复chunk冲突（`--on-conflict=fail`时） |
| 6 | 其他错误 |

### `rag query` - 查询RAG系统

**参数说明：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `QUERY_TEXT` | 位置参数 | - | 查询问题（可选，默认从stdin读取） |
| `--index-dir, -i` | 选项 | `.rag_index` | 索引存储目录 |
| `--top-k, -k` | 选项 | 5 | 返回的结果数量 |
| `--embed-backend, -e` | 选项 | `stub` | 嵌入后端 |
| `--gen-backend, -g` | 选项 | `stub` | 生成后端 |
| `--rerank/--no-rerank` | 标志 | `--rerank` | 是否启用重排 |
| `--reranker` | 选项 | `tf_idf` | 重排器：`tf_idf` 或 `length_penalty` |
| `--max-context-chars` | 选项 | 8000 | 上下文最大字符数 |

**输出格式（JSON）：**

```json
{
  "answer": "RAG 代表 Retrieval-Augmented Generation...",
  "cites": [
    {
      "chunk_id": "rag_intro.txt:0:100",
      "source": "rag_intro.txt",
      "file_path": "/path/to/data/rag_intro.txt",
      "offset_start": 0,
      "offset_end": 100,
      "reference_index": 1
    }
  ],
  "latency_ms": 150,
  "embed_backend": "stub",
  "gen_backend": "stub"
}
```

**退出码含义：**

| 退出码 | 含义 |
|--------|------|
| 0 | 成功 |
| 1 | 未提供查询文本 |
| 2 | JSON输入格式错误 |
| 3 | 索引不存在或为空 |
| 4-7 | 后端初始化错误 |

## 后端配置

### 嵌入后端

#### 存根后端（默认）

生成基于SHA-256哈希的确定性伪向量，用于测试和开发。

```bash
rag ingest ./data --embed-backend stub --embed-dim 128
```

#### OpenAI后端

需要设置`OPENAI_API_KEY`环境变量：

```bash
export OPENAI_API_KEY="your-api-key"
rag ingest ./data --embed-backend openai
```

### 生成后端

#### 存根后端（默认）

输出确定性、可解析的答案结构，包含引用信息。

```bash
rag query --gen-backend stub "你的问题"
```

#### OpenAI后端

```bash
export OPENAI_API_KEY="your-api-key"
rag query --gen-backend openai "你的问题"
```

## 重排器

支持两种轻量级重排器：

### TF-IDF重排器（默认）

根据查询词在chunk中的出现频率调整分数。

```bash
rag query --reranker tf_idf "你的问题"
```

### 长度惩罚重排器

偏好中等长度的chunk，惩罚过短或过长的文本。

```bash
rag query --reranker length_penalty "你的问题"
```

## 测试

### 运行所有测试

```bash
pytest

# 或带详细输出
pytest -v
```

### 测试覆盖的场景

| 测试模块 | 覆盖场景 |
|----------|----------|
| `test_chunker.py` | 空文档、单chunk、多chunk、overlap关系、UTF-8多字节字符 |
| `test_embedding.py` | 确定性向量、归一化、不同文本不同向量 |
| `test_retriever.py` | 重排开关效果、空索引检索、不同重排器 |
| `test_generator.py` | 无结果生成、引用格式、上下文截断警告 |
| `test_cli.py` | 不存在目录、空目录、空索引查询、完整流程 |

### 关键边界情况测试

- **空语料**：`ingest`空目录时返回非零退出码
- **空索引查询**：`query`空索引时返回非零退出码
- **UTF-8边界**：切片不会在多字节字符中间截断
- **上下文超长**：超出`max_context_chars`时打印警告并截断
- **引用锚点**：所有生成结果都包含可追溯的chunk引用

## 项目结构

```
15-llm-rag-pipeline/
├── pyproject.toml          # 项目配置和依赖
├── README.md              # 本文档
├── src/
│   └── llm_rag_pipeline/
│       ├── __init__.py    # 包初始化
│       ├── cli.py         # CLI入口（ingest/query命令）
│       ├── loader.py      # 文档加载和解析
│       ├── chunker.py     # 文本切片
│       ├── embedding.py   # 嵌入后端
│       ├── index_store.py # 向量索引存储
│       ├── retriever.py   # 检索和重排
│       └── generator.py   # 生成后端
└── tests/
    ├── __init__.py
    ├── test_chunker.py
    ├── test_embedding.py
    ├── test_retriever.py
    ├── test_generator.py
    └── test_cli.py
```

## 常见问题

### Q: 为什么使用存根后端？

存根后端提供：
- **确定性**：同一文本始终生成相同向量，便于测试
- **无需API**：本地即可运行，无需外部依赖
- **可预测性**：输出结构固定，便于集成测试

### Q: 如何切换到真实模型？

设置环境变量后使用对应后端：

```bash
export OPENAI_API_KEY="sk-..."

# 向量化
rag ingest ./data --embed-backend openai

# 生成
rag query --embed-backend openai --gen-backend openai "问题"
```

### Q: 索引文件存储在哪里？

默认存储在当前目录的`.rag_index`文件夹中，可以通过`--index-dir`参数或`RAG_INDEX_DIR`环境变量自定义。

### Q: 如何处理索引兼容性？

索引元数据包含：
- 向量维度
- 嵌入后端名称
- 版本号

如果新数据与现有索引不兼容，`ingest`命令会返回非零退出码并提示错误。可以使用`--clear`参数清空后重建。

## 许可证

MIT License
